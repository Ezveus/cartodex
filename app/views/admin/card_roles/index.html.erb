<%= render Admin::CardRoles::IndexView.new(
      rows: @rows, unfingerprinted: @unfingerprinted, roles: @roles, assignments: @assignments,
      page: @page, pages: @pages,
      filters: { q: @query.presence, card_type: @card_type, played: @played }.compact) %>
