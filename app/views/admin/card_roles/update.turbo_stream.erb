<%# The row and only the row: what the reader sees is the decision the database now holds, not
    the box the browser ticked. %>
<%= turbo_stream.replace Admin::CardRoles::Row.dom_id(@card.fingerprint) do %>
  <%= render Admin::CardRoles::Row.new(card: @card, roles: @roles, assignments: @assignments) %>
<% end %>
