<%= render Admin::StandingsImports::NewView.new(source: @source, tournament_id: @tournament_id, deck_id: @deck_id, slug: @slug, rotation: @rotation, set: @set, archetype_id: @archetype_id, event_filters: @event_filters_text, limit_per_event: @limit_per_event_text, archetypes: @archetypes, plan: @plan, archetype: @archetype, mapping_lines: @mapping_lines) %>
