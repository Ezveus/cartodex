<%= render Cards::IndexView.new(
  blocks: @blocks,
  current_set: @current_set,
  cards: @cards,
  query: @query,
  type: @type,
  energy: @energy,
  rarity: @rarity,
  mark: @mark,
  label: @label,
  role: @role,
  rarities: @rarities,
  marks: @marks,
  labels: @labels,
  roles: @roles,
  searching: @searching,
  card_counts: @card_counts,
  total: @total,
  page: @page,
  pages: @pages
) %>
