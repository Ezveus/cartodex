<%= render Cards::IndexView.new(
  blocks: @blocks,
  current_set: @current_set,
  cards: @cards,
  query: @query,
  type: @type,
  energy: @energy,
  rarity: @rarity,
  mark: @mark,
  rarities: @rarities,
  marks: @marks,
  searching: @searching,
  card_counts: @card_counts,
  total: @total,
  page: @page,
  pages: @pages
) %>
