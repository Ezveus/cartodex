<%= render Decks::ShowView.new(
  deck: @deck,
  editing: @editing,
  tournament_profiles: @tournament_profiles,
  availability: @availability || {},
  over_allocated_card_ids: @over_allocated_card_ids || [],
  swappable_card_ids: @swappable_card_ids || []
) %>
